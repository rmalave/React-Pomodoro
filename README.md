## Pmodoro timer and todo list with ReactJS

### Simple pomodoro timer build with ReactJS

### To use
Clone this repository to your local machine
run `npm install`
run `npm run start`

### Tests
run tests with `npm run test`
